<UserControl x:Class="TruckPreparer.SpecialArea.SpecialViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TruckPreparer.SpecialArea"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="300">
    <Grid>
        <StackPanel>
        <Label FontFamily="Arial" FontSize="24" HorizontalAlignment="Center">Special Areas</Label>
        <DockPanel>
        <ListBox Name="Special_LB" MinHeight="300" DockPanel.Dock="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal">
                <Button Name="Add_Button" Content="Add" Click="Add_Button_Click"></Button>
                <Button Name="Remove_Button" Content="Remove"></Button>
                <Button Name="Edit_Button" Content="Edit" Click="Edit_Button_Click"></Button>
        </StackPanel>
        </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
